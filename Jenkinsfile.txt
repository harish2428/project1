pipeline {
    agent any
    
    environment {
        TOMCAT_URL = 'http://192.168.56.1:8081/'
        WAR_FILE = '"C:/project1/SampleWebApp.war"'
        TOMCAT_USER = credentials('harish')
        TOMCAT_PASS = credentials('bvbschool@24')
    }

    stages {
        stage('Deploy to Tomcat') {
            steps {
                script {
                    def tomcat = Tomcat.tomcat(url: TOMCAT_URL, credentialsId: TOMCAT_USER)
                    tomcat.deploy contextPath: '/', war: WAR_FILE
                }
            }
        }
    }
}
